<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Page Title</title>

</head>
<body>
    <header>
        <nav>
            <h1>Edit Photo</h1>
            <a href='/photos'><button>Home</button></a>
        </nav>
    </header>
    <main>
        <form action='/photos/<%=photo._id%>?_method=PUT' method='POST'>
            User:
            <select name='userId'>
            <%for(let i = 0; i < users.length; i++) {%>
                <option value='<%=users[i]._id%>'><%=users[i].username%></option>
          <% }%>
          </select>
            Title:<input type='text' name='title' value='<%=photo.title%>'><br>
            Image:<input type='text' name='image'><br>

            <button type='submit'>Submit Changes</button>
        </form>
    </main>
</body>
</html>